import("//build/ohos.gni")
import("device.gni")

print("dlmp135 in")
group("dlmp135") {
    deps = [
        "fip:fip",
        "fip:flashlayout",
        # "bootfs:bootfs",
    ]
}

group("make_bootfs") {
    deps = [
        "bootfs:bootfs",
    ]
}
